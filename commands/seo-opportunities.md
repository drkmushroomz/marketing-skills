Analyze SEO opportunities for jetfuel.agency.

Read the full skill prompt at `.claude/ops/seo-opportunities/PROMPT.md` and config at `.claude/ops/seo-opportunities/config.json`, then execute each step.

Use the Python script at `scripts/seo_opportunities.py` to pull GSC data and run the analysis. If the script doesn't exist yet, create it based on the PROMPT.md instructions.

Write results to a Google Sheet using the google-workspace MCP tools (create_spreadsheet, modify_sheet_values).

$ARGUMENTS
